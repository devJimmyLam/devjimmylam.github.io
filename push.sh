#!/usr/bin/env bash

git add . --all

# NOTE: Your commit message must be written in the following format "{{ your initial } - {{ your commit message goes here... }}"
git commit -m "deployed flowerseverafter project"

git status

git push